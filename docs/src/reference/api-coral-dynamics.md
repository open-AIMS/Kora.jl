# Coral Dynamics API

The coral dynamics functions (`apply_growth!`, `apply_survival!`, `bleaching_mortality!`, `larval_production`) are internal implementation details called by [`run_model!`](@ref). They are not part of the public API.
