# Coral Models API

`PolyGrowthModel` and `PolySurvivalModel` are the concrete model types returned by [`load_models`](@ref) and accepted by [`initialize_reef`](@ref). They are opaque containers. Interact with them through the I/O and simulation functions rather than constructing them directly.
